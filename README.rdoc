### Barebone exmaple of using omniauth-facebook with Rails 

+ localhost:3000/login
+ localhost:3000/logout  
+ localhost:3000/auth/facebook 

Important note: if you are using Mac OS X mountain lion you might need to run this first.
`brew list curl-ca-bundle`
